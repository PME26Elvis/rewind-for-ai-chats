export function renderWizardSourceSelection({ selectedCount, selectionLabel }) {
  return `<div class="card" style="margin-top: 24px"><h3>Source selection</h3><p>Choose JSON files/folders or browser-saved HTML files/folders for ChatGPT and Gemini.</p><p><strong>${selectedCount}</strong> source${selectedCount === 1 ? '' : 's'} currently selected.</p>${selectionLabel ? `<p style="color: #cbd5e1">${selectionLabel}</p>` : ''}<div class="button-row"><label class="button-like">Choose JSON/HTML files<input id="import-file-input" type="file" accept="application/json,.json,text/html,.html" multiple hidden /></label><label class="button-like">Choose JSON/HTML folder<input id="import-folder-input" type="file" accept="application/json,.json,text/html,.html" webkitdirectory directory hidden /></label><button id="detect-button" type="button">Refresh detect summary</button><button id="execute-button" type="button">Execute import</button><button id="reset-button" type="button">Reset</button></div></div>`;
}
